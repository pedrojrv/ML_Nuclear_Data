# Set up a YAML file to specify your training script, parameter ranges, search strategy, 
# and stopping criteria. W&B will pass these parameters and their values as command line 
# arguments to your training script, and we'll automatically parse them with the config 
# object you set up in Step 1.

project: ml-nuclear-sweeps
entity: pvicentevaldez
program: wandb_train.py
method: bayes
metric:
  name: loss
  goal: minimize
parameters:
  layer_1_size:
    min: 70
    max: 1000
  layer_2_size:
    min: 70
    max: 1000
  layer_3_size:
    min: 70
    max: 1000
  layer_4_size:
    min: 70
    max: 1000
  layer_5_size:
    min: 70
    max: 1000
  layer_6_size:
    min: 70
    max: 1000
  layer_7_size:
    min: 70
    max: 1000
  layer_8_size:
    min: 70
    max: 1000
  layer_9_size:
    min: 70
    max: 1000
  layer_10_size:
    min: 70
    max: 1000
  learning_rate:
    min: 0.0005
    max: 0.005
    distribution: log_uniform
  lr_schedule_type:
    values: ["ITD", "plateau", "normal"]
  lr_decay_epochs:
    values: [0, 10, 20]